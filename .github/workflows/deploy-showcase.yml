name: Deploy Showcase

on:
  push:
    branches: [main]
    paths:
      - "docs/showcase/**"
      - ".github/workflows/deploy-showcase.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # A repository Pages site can be created automatically only with an
          # administrator PAT. Existing Pages sites use the workflow token.
          token: ${{ secrets.PAGES_ADMIN_TOKEN || github.token }}
          enablement: ${{ secrets.PAGES_ADMIN_TOKEN != '' }}
      - name: Select GitHub Actions as Pages source
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAGES_ADMIN_TOKEN || github.token }}
          script: |
            await github.rest.repos.updateInformationAboutPagesSite({
              owner: context.repo.owner,
              repo: context.repo.repo,
              build_type: "workflow",
            });
      - name: Upload showcase
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/showcase

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
